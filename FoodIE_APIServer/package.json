{
  "name": "apiserver",
  "version": "1.0.0",
  "description": "API server for FoodIE",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "weihan",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "express-http-proxy": "^1.5.1"
  }
}
